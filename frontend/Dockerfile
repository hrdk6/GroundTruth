# Placeholder until Phase 6. Kept so the `frontend` compose profile has a valid
# build context; it is never built by `make up`.
FROM node:22-alpine
WORKDIR /app
CMD ["sh", "-c", "echo 'Frontend not implemented until Phase 6.' && sleep infinity"]
